apiVersion: v1
kind: Service
metadata:
  name: {{ include "mac-monitor-charts.fullname" . }}
  labels:
    {{- include "mac-monitor-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mac-monitor-charts.selectorLabels" . | nindent 4 }}
