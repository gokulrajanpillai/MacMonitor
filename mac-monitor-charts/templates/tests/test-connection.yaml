apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mac-monitor-charts.fullname" . }}-test-connection"
  labels:
    {{- include "mac-monitor-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mac-monitor-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
